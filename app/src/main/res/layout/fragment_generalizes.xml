<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tl="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <RelativeLayout
        android:id="@+id/rl_genera"
        android:layout_width="match_parent"
        android:layout_height="68dp"
        android:background="#D81717">

        <com.flyco.tablayout.SegmentTabLayout
            android:id="@+id/segment_tab"
            android:layout_centerHorizontal="true"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="30dp"
            android:paddingLeft="10dp"
            android:paddingRight="10dp"
            tl:tl_indicator_anim_enable="true"
            tl:tl_indicator_bounce_enable="false"
            tl:tl_textSelectColor="#D81717"
            tl:tl_textUnselectColor="#fff"
            tl:tl_bar_stroke_color="#99ffffff"
            tl:tl_indicator_color="#fff"
            tl:tl_bar_color="#99ffffff"
            tl:tl_tab_padding="18dp" />

    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/rl_person"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <android.support.v4.view.ViewPager
        android:id="@+id/vp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

</LinearLayout>